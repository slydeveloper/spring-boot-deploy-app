### Kubernetes deployment example

#### Requirements
* Minikube
    * tested on `v1.3.1`

#### Deployment
1. Start Minikube
    * `$ ./k8s-minikube-start.sh`
2. Deploy application on K8S
    * `$ ./k8s-deploy.sh`
3. Update version in `sbda-app-deployment.yaml`
    * i.e `image: slydeveloper/spring-boot-deploy-app:2.0`
4. Apply update application on K8S
    * `$ ./k8s-update.sh`

